#include<stdio.h>
#include<string.h>
int main(){
    int n;
    char arr[n];
    for(int i=0;i<n;i++){
        scanf("%s",arr[i]);
    }

    for(int i=0;i<n;i++){
        printf("%s" ,arr[i]);
    }
}